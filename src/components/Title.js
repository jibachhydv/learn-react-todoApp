import React, { Component } from "react";

class Title extends React.Component {
  render() {
    return <h1 style={styles}>CS50 Todo App</h1>;
  }
}

styles = {
  color: "blue"
};

export default Title;
